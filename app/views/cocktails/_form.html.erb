<%= simple_form_for(cocktail) do |form| %>
  <% if cocktail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cocktail.errors.count, "error") %> prohibited this cocktail from being saved:</h2>

      <ul>
      <% cocktail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.input :name, label: "Cocktail name:" %>
  </div>

  <div class="field">
    <%= form.input :photo %>
    <%= form.input :photo_cache, as: :hidden %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>